
          {/* Step 1: Configure Company (Full page layout) */}
          {step === 1 && (
            <div
              className="flex flex-col items-center w-full h-full"
              style={{ backgroundColor: '#ffffff', animation: 'fadeIn 0.4s ease' }}
            >
              {/* Header */}
              <div className="w-full flex items-center justify-between px-10 py-8">
                <div className="flex items-center gap-4">
                  {/* Real Logo overlapping circles matching Phase 0 */}
                  <div style={{ position: 'relative', flexShrink: 0, marginTop: 4 }}>
                    <div style={{
                      width: 60, height: 60, borderRadius: '50%',
                      backgroundColor: getAvatarColor(companyName || 'Dort Asia').bg, overflow: 'hidden',
                      display: 'flex', alignItems: 'center', justifyContent: 'center', border: '1px solid rgba(0,0,0,0.05)'
                    }}>
                      {(croppedPreviewUrl || profilePic) ? (
                        <img src={croppedPreviewUrl ?? profilePic!} style={{ width: '100%', height: '100%', objectFit: 'cover' }} alt="Company" />
                      ) : (
                        <span className="avatar-initials" style={{ fontSize: 20, fontWeight: 800, color: getAvatarColor(companyName || 'Dort Asia').color }}>
                          {getInitials(companyName || 'DA')}
                        </span>
                      )}
                    </div>
                    <div style={{
                      position: 'absolute', bottom: -2, right: -2,
                      width: 26, height: 26, borderRadius: '50%',
                      backgroundColor: '#ffffff', display: 'flex',
                      alignItems: 'center', justifyContent: 'center', padding: 2,
                    }}>
                      <div style={{
                        width: '100%', height: '100%', borderRadius: '50%',
                        backgroundColor: getAvatarColor(`${adminFirstName} ${adminLastName}`.trim() || companyName || 'Dort Asia').bg, overflow: 'hidden',
                        display: 'flex', alignItems: 'center', justifyContent: 'center',
                      }}>
                        {(adminCroppedUrl || adminPic) ? (
                          <img src={adminCroppedUrl ?? adminPic!} style={{ width: '100%', height: '100%', objectFit: 'cover' }} alt="User" />
                        ) : (
                          <span className="avatar-initials" style={{ fontSize: 9, fontWeight: 800, color: getAvatarColor(`${adminFirstName} ${adminLastName}`.trim() || companyName || 'Dort Asia').color }}>
                            {adminFirstName
                              ? (adminFirstName[0] + (adminLastName?.[0] || '')).toUpperCase()
                              : getInitials(companyName || 'DA')}
                          </span>
                        )}
                      </div>
                    </div>
                  </div>
                  <div className="flex flex-col gap-0.5">
                    <span className="onboarding-dark-gray-text text-[11px] font-medium leading-none">Company</span>
                    <span className="text-[#000000] text-[16px] font-bold leading-none">{companyName || 'Dort Asia'}</span>
                  </div>
                </div>
                <div className="text-[14px] font-medium text-[#8E8E93]">
                  Phase : <span className="text-[#007AFF]">1 out of 5</span>
                </div>
              </div>

              {/* Main Content */}
              <div className="flex flex-col items-center justify-center w-full max-w-[480px] px-4 mt-[10vh]">
                <h1 className="text-[22px] font-bold text-[#000000] mb-2">Configure Company</h1>
                <p className="onboarding-gray-text text-[14px] font-medium mb-10">Configure Your Company to get started</p>
                
                <div className="w-full space-y-6">
                  {/* Time Inputs */}
                  <div className="grid grid-cols-2 gap-6">
                    <div className="relative">
                      <label className="absolute -top-[9px] left-4 bg-[#ffffff] px-1 text-[11px] font-semibold text-[#000000] z-10 transition-all pointer-events-none">
                        Start Time
                      </label>
                      <input 
                        type="text"
                        placeholder="HH : MM"
                        value={startTime}
                        onChange={(e) => setStartTime(formatTimeInput(e.target.value))}
                        className="w-full pl-5 pr-[80px] py-[18px] bg-[#ffffff] shadow-none border border-[#E5E5EA] rounded-[20px] text-[#1C1C1E] text-[14px] font-medium focus:outline-none focus:border-[#007AFF] focus:ring-1 focus:ring-[#007AFF]/20 transition-all placeholder:text-[#C7C7CC]"
                      />
                      <div className="absolute right-2.5 top-[10px] bottom-[10px] !bg-[#F2F2F7] rounded-full p-[3px] flex items-center">
                        <button 
                          onClick={() => setStartAmPm("AM")}
                          className={`px-3 py-[6px] rounded-full text-[13px] font-medium transition-all ${startAmPm === 'AM' ? 'bg-white text-[#1C1C1E] !shadow-none ring-0' : 'text-[#8E8E93] hover:text-[#1C1C1E]'}`}
                        >
                          AM
                        </button>
                        <button 
                          onClick={() => setStartAmPm("PM")}
                          className={`px-3 py-[6px] rounded-full text-[13px] font-medium transition-all ${startAmPm === 'PM' ? 'bg-white text-[#1C1C1E] !shadow-none ring-0' : 'text-[#8E8E93] hover:text-[#1C1C1E]'}`}
                        >
                          PM
                        </button>
                      </div>
                    </div>
                    <div className="relative">
                      <label className="absolute -top-[9px] left-4 bg-[#ffffff] px-1 text-[11px] font-semibold text-[#000000] z-10 transition-all pointer-events-none">
                        End Time
                      </label>
                      <input 
                        type="text"
                        placeholder="HH : MM"
                        value={endTime}
                        onChange={(e) => setEndTime(formatTimeInput(e.target.value))}
                        className="w-full pl-5 pr-[80px] py-[18px] bg-[#ffffff] shadow-none border border-[#E5E5EA] rounded-[20px] text-[#1C1C1E] text-[14px] font-medium focus:outline-none focus:border-[#007AFF] focus:ring-1 focus:ring-[#007AFF]/20 transition-all placeholder:text-[#C7C7CC]"
                      />
                      <div className="absolute right-2.5 top-[10px] bottom-[10px] !bg-[#F2F2F7] rounded-full p-[3px] flex items-center">
                        <button 
                          onClick={() => setEndAmPm("AM")}
                          className={`px-3 py-[6px] rounded-full text-[13px] font-medium transition-all ${endAmPm === 'AM' ? 'bg-white text-[#1C1C1E] !shadow-none ring-0' : 'text-[#8E8E93] hover:text-[#1C1C1E]'}`}
                        >
                          AM
                        </button>
                        <button 
                          onClick={() => setEndAmPm("PM")}
                          className={`px-3 py-[6px] rounded-full text-[13px] font-medium transition-all ${endAmPm === 'PM' ? 'bg-white text-[#1C1C1E] !shadow-none ring-0' : 'text-[#8E8E93] hover:text-[#1C1C1E]'}`}
                        >
                          PM
                        </button>
                      </div>
                    </div>
                  </div>

                  {/* Company Type Dropdown */}
                  <div className="relative">
                    <label className="absolute -top-[9px] left-4 bg-[#ffffff] px-1 text-[11px] font-semibold text-[#000000] z-10 transition-all pointer-events-none">
                      Company Type
                    </label>
                    <select
                      value={companyType}
                      onChange={(e) => setCompanyType(e.target.value)}
                      className={`w-full px-5 py-[18px] bg-[#ffffff] shadow-none border border-[#E5E5EA] rounded-[20px] text-[14px] font-medium focus:outline-none focus:border-[#007AFF] focus:ring-1 focus:ring-[#007AFF]/20 transition-all appearance-none ${companyType ? 'text-[#1C1C1E]' : 'text-[#C7C7CC] is-placeholder'}`}
                    >
                      <option value="" disabled className="text-[#C7C7CC]">Choose Company Type</option>
                      <option value="Technology" className="text-[#1C1C1E]">Technology</option>
                      <option value="Business & Trade" className="text-[#1C1C1E]">Business & Trade</option>
                      <option value="Services" className="text-[#1C1C1E]">Services</option>
                      <option value="Creative & Media" className="text-[#1C1C1E]">Creative & Media</option>
                      <option value="Education" className="text-[#1C1C1E]">Education</option>
                      <option value="Health & Wellness" className="text-[#1C1C1E]">Health & Wellness</option>
                      <option value="Food & Hospitality" className="text-[#1C1C1E]">Food & Hospitality</option>
                      <option value="Manufacturing & Industry" className="text-[#1C1C1E]">Manufacturing & Industry</option>
                      <option value="Non-Profit & Government" className="text-[#1C1C1E]">Non-Profit & Government</option>
                      <option value="Other" className="text-[#1C1C1E]">Other</option>
                    </select>
                    <div className="absolute right-5 top-1/2 -translate-y-1/2 pointer-events-none">
                      <ChevronDown className="h-5 w-5 text-[#8E8E93]" />
                    </div>
                  </div>

                  {companyType === 'Other' && (
                    <div className="relative animate-in slide-in-from-top-2 fade-in duration-300">
                      <label className="absolute -top-[9px] left-4 bg-[#ffffff] px-1 text-[11px] font-semibold text-[#000000] z-10 transition-all pointer-events-none">
                        Specify Other Type
                      </label>
                      <input
                        type="text"
                        placeholder="E.g., Logistics, Real Estate..."
                        value={customCompanyType}
                        onChange={(e) => setCustomCompanyType(e.target.value)}
                        className="w-full px-5 py-[18px] bg-[#ffffff] shadow-none border border-[#E5E5EA] rounded-[20px] text-[#1C1C1E] text-[14px] font-medium focus:outline-none focus:border-[#007AFF] focus:ring-1 focus:ring-[#007AFF]/20 transition-all placeholder:text-[#C7C7CC]"
                      />
                    </div>
                  )}

                  {/* Attendance Type Dropdown */}
                  <div className="relative">
                    <label className="absolute -top-[9px] left-4 bg-[#ffffff] px-1 text-[11px] font-semibold text-[#000000] z-10 transition-all pointer-events-none">
                      Attendance Type
                    </label>
                    <select
                      value={attendanceType}
                      onChange={(e) => setAttendanceType(e.target.value)}
                      className={`w-full px-5 py-[18px] bg-[#ffffff] shadow-none border border-[#E5E5EA] rounded-[20px] text-[14px] font-medium focus:outline-none focus:border-[#007AFF] focus:ring-1 focus:ring-[#007AFF]/20 transition-all appearance-none ${attendanceType ? 'text-[#1C1C1E]' : 'text-[#C7C7CC] is-placeholder'}`}
                    >
                      <option value="" disabled className="text-[#C7C7CC]">Choose Attendance Type</option>
                      <option value="Mobile Attendance" className="text-[#1C1C1E]">Mobile Attendance</option>
                      <option value="Manual Attendance" className="text-[#1C1C1E]">Manual Attendance</option>
                      <option value="Web Attendance" className="text-[#1C1C1E]">Web Attendance</option>
                    </select>
                    <div className="absolute right-5 top-1/2 -translate-y-1/2 pointer-events-none">
                      <ChevronDown className="h-5 w-5 text-[#8E8E93]" />
                    </div>
                  </div>
                </div>

                <div className="mt-10 w-full flex flex-col items-center">
                  <div className="h-4 mb-2 flex items-center justify-center">
                    {errorLine && <span className="text-[12px] font-semibold text-[#FF3B30] slide-in-from-bottom-2 animate-in fade-in">{errorLine}</span>}
                  </div>
                  <button
                    onClick={handleNext}
                    className="w-[180px] py-[14px] bg-[#007AFF] hover:bg-[#0062CC] active:scale-[0.98] rounded-[14px] text-white font-bold text-[15px] transition-all"
                  >
                    Next
                  </button>
                </div>
              </div>
            </div>
          )}

          {/* Step 2: Onboard Departments */}
          {step === 2 && (
            <div className="w-full h-full flex flex-col bg-[#ffffff] animate-in fade-in duration-500">
              {/* Header */}
              <div className="w-full flex items-center justify-between px-10 py-8">
                <div className="flex items-center gap-4">
                  {/* Real Logo overlapping circles matching Phase 0 */}
                  <div style={{ position: 'relative', flexShrink: 0, marginTop: 4 }}>
                    <div style={{
                      width: 60, height: 60, borderRadius: '50%',
                      backgroundColor: getAvatarColor(companyName || 'Dort Asia').bg, overflow: 'hidden',
                      display: 'flex', alignItems: 'center', justifyContent: 'center', border: '1px solid rgba(0,0,0,0.05)'
                    }}>
                      {(croppedPreviewUrl || profilePic) ? (
                        <img src={croppedPreviewUrl ?? profilePic!} style={{ width: '100%', height: '100%', objectFit: 'cover' }} alt="Company" />
                      ) : (
                        <span className="avatar-initials" style={{ fontSize: 20, fontWeight: 800, color: getAvatarColor(companyName || 'Dort Asia').color }}>
                          {getInitials(companyName || 'DA')}
                        </span>
                      )}
                    </div>
                    <div style={{
                      position: 'absolute', bottom: -2, right: -2,
                      width: 26, height: 26, borderRadius: '50%',
                      backgroundColor: '#ffffff', display: 'flex',
                      alignItems: 'center', justifyContent: 'center', padding: 2,
                    }}>
                      <div style={{
                        width: '100%', height: '100%', borderRadius: '50%',
                        backgroundColor: getAvatarColor(`${adminFirstName} ${adminLastName}`.trim() || companyName || 'Dort Asia').bg, overflow: 'hidden',
                        display: 'flex', alignItems: 'center', justifyContent: 'center',
                      }}>
                        {(adminCroppedUrl || adminPic) ? (
                          <img src={adminCroppedUrl ?? adminPic!} style={{ width: '100%', height: '100%', objectFit: 'cover' }} alt="User" />
                        ) : (
                          <span className="avatar-initials" style={{ fontSize: 9, fontWeight: 800, color: getAvatarColor(`${adminFirstName} ${adminLastName}`.trim() || companyName || 'Dort Asia').color }}>
                            {adminFirstName
                              ? (adminFirstName[0] + (adminLastName?.[0] || '')).toUpperCase()
                              : getInitials(companyName || 'DA')}
                          </span>
                        )}
                      </div>
                    </div>
                  </div>
                  <div className="flex flex-col gap-0.5">
                    <span className="onboarding-dark-gray-text text-[11px] font-medium leading-none">Company</span>
                    <span className="text-[#1C1C1E] text-[15px] font-bold leading-none">{companyName || 'Dort Asia'}</span>
                  </div>
                </div>
                
                <div className="flex items-center gap-2 text-[14px]">
                  <span className="text-[#8E8E93] font-medium">Phase : </span>
                  <span className="text-[#007AFF] font-bold">2 out of 5</span>
                </div>
              </div>

              {/* Main Dual-Column Content */}
              <div className="flex w-full flex-1 border-t border-[#E5E5EA] overflow-hidden">
                {/* Left Column: Created Departments */}
                <div className="w-[50%] flex flex-col px-[55px] py-[35px] border-r border-[#E5E5EA] h-full overflow-hidden">
                  <h2 className="text-[17px] font-bold text-[#1C1C1E] mb-1 shrink-0">
                    Created Departments <span className="text-[#007AFF] text-[11px] font-bold tracking-wide ml-1">(Max 5 While Setup)</span>
                  </h2>
                  <p className="text-[12.5px] text-[#8E8E93] font-medium mb-8 shrink-0">The Departments Can be update and Configure after setup</p>
                  
                  <div className="flex-1 overflow-y-auto w-full space-y-4 no-scrollbar pr-2">
                    {createdDepartments.map((dept) => {
                      const avatarProps = getAvatarColor(dept.name);
                      return (
                        <div key={dept.id} className="w-full !bg-[#F8F9FA] rounded-[18px] p-4 px-5 flex items-center justify-between shadow-none border-none group">
                          <div className="flex items-center gap-4">
                            <div className="w-[44px] h-[44px] rounded-full flex items-center justify-center font-bold text-[15px] shrink-0" style={{ backgroundColor: avatarProps.bg, color: avatarProps.color }}>
                              {getInitials(dept.name)}
                            </div>
                            <div className="flex flex-col">
                              <span className="text-[15px] font-semibold !text-[#1C1C1E]">{dept.name}</span>
                              <span className="text-[13px] !text-[#A1A1A6] font-medium mt-[1px]">{dept.description || 'Descritption'}</span>
                            </div>
                          </div>
                          
                          <div className="flex items-center gap-12 shrink-0 pr-1">
                            <span className="text-[14px] font-medium !text-[#1C1C1E]">{dept.designations.length} Designation Created</span>
                            <button 
                              onClick={() => handleRemoveDepartment(dept.id)} 
                              className="text-[#FF3B30] hover:scale-110 active:scale-95 transition-all p-1"
                            >
                              <Trash2 className="w-[20px] h-[20px] stroke-[1.5]" />
                            </button>
                          </div>
                        </div>
                      )
                    })}
                    
                    {createdDepartments.length === 0 && (
                      <div className="w-full h-[100px] border-2 border-dashed border-[#E5E5EA] rounded-[18px] flex items-center justify-center">
                        <span className="text-[#8E8E93] text-[13px] font-medium">No departments created yet</span>
                      </div>
                    )}
                  </div>
                </div>
                
                {/* Right Column: Add Form */}
                <div className="w-[50%] flex flex-col justify-between px-[55px] py-[35px] relative h-full overflow-hidden">
                  <div className="flex flex-col items-center w-full min-h-0 overflow-y-auto no-scrollbar">
                    <h1 className="text-[22px] font-bold text-[#000000] mb-2 text-center mt-6">Onboard Departments</h1>
                    <p className="onboarding-gray-text text-[14px] font-medium mb-10 text-center">Create Department to deploy Employees</p>
                    
                    <div className="w-full max-w-[480px] space-y-7 pb-[120px]">
                      {/* Department Name Input */}
                      <div className="relative">
                        <label className="absolute -top-[9px] left-4 bg-[#ffffff] px-1 text-[11px] font-semibold text-[#000000] z-10 transition-all pointer-events-none">
                          Department Name
                        </label>
                        <input 
                          type="text"
                          placeholder="Enter Name"
                          value={deptName}
                          onChange={(e) => setDeptName(e.target.value)}
                          className="w-full px-5 py-[18px] bg-[#ffffff] shadow-none border border-[#E5E5EA] rounded-[20px] text-[#1C1C1E] text-[14px] font-medium focus:outline-none focus:border-[#007AFF] focus:ring-1 focus:ring-[#007AFF]/20 transition-all placeholder:text-[#C7C7CC]"
                        />
                      </div>

                      {/* Description Input */}
                      <div className="relative">
                        <label className="absolute -top-[9px] left-4 bg-[#ffffff] px-1 text-[11px] font-semibold text-[#000000] z-10 transition-all pointer-events-none">
                          Description
                        </label>
                        <input 
                          type="text"
                          maxLength={100}
                          placeholder="About Department"
                          value={deptDesc}
                          onChange={(e) => setDeptDesc(e.target.value)}
                          className="w-full px-5 py-[18px] bg-[#ffffff] shadow-none border border-[#E5E5EA] rounded-[20px] text-[#1C1C1E] text-[14px] font-medium focus:outline-none focus:border-[#007AFF] focus:ring-1 focus:ring-[#007AFF]/20 transition-all placeholder:text-[#C7C7CC]"
                        />
                      </div>

                      {/* Designation Input with Inline Add */}
                      <div>
                        <div className="relative flex items-center gap-3">
                          <div className="relative flex-1">
                            <label className="absolute -top-[9px] left-4 bg-[#ffffff] px-1 text-[11px] font-semibold text-[#000000] z-10 transition-all pointer-events-none">
                              Designation
                            </label>
                            <input 
                              type="text"
                              placeholder="Type Designation & Press Enter"
                              value={desigName}
                              onChange={(e) => setDesigName(e.target.value)}
                              onKeyDown={(e) => { 
                                if (e.key === 'Enter') { 
                                  e.preventDefault(); 
                                  if (desigName.trim()) {
                                    handleAddDesignation();
                                  } else if (deptName.trim() && createdDepartments.length < 5) {
                                    handleDeployDepartment();
                                  }
                                } 
                              }}
                              className="w-full px-5 py-[18px] bg-[#ffffff] shadow-none border border-[#E5E5EA] rounded-[20px] text-[#1C1C1E] text-[14px] font-medium focus:outline-none focus:border-[#007AFF] focus:ring-1 focus:ring-[#007AFF]/20 transition-all placeholder:text-[#C7C7CC]"
                            />
                          </div>
                          <button 
                            onClick={handleDeployDepartment}
                            disabled={!deptName.trim() || createdDepartments.length >= 5}
                            className="px-[25px] py-[18px] shrink-0 bg-[#007AFF] hover:bg-[#0062CC] active:scale-[0.97] text-white font-bold rounded-[20px] text-[14px] shadow-sm transition-all disabled:opacity-50 disabled:cursor-not-allowed"
                          >
                            Add
                          </button>
                        </div>
                        
                        {/* Designation Pills */}
                        {designations.length > 0 && (
                          <div className="flex flex-wrap gap-2.5 mt-4 ml-1 max-h-[140px] overflow-y-auto page-scrollbar pr-2 pb-1">
                            {designations.map(d => (
                              <div key={d} className="flex items-center gap-3 !bg-[#F2F2F7] px-3.5 py-1.5 rounded-full animate-in fade-in zoom-in-95 duration-200">
                                <span className="text-[12px] font-medium !text-[#1C1C1E]">{d}</span>
                                <button onClick={() => handleRemoveDesignation(d)} className="!text-[#A1A1A6] hover:!text-[#1C1C1E] transition-colors flex items-center justify-center">
                                  <X className="w-[12px] h-[12px] stroke-[2.5]" style={{ color: 'inherit' }} />
                                </button>
                              </div>
                            ))}
                          </div>
                        )}
                      </div>
                    </div>
                  </div>

                  {/* Bottom Navigation fixed at the bottom right corner of the col */}
                  <div className="absolute bottom-[35px] left-[55px] right-[55px] flex justify-between items-center bg-[#ffffff] pt-4">
                    <div className="flex gap-8">
                      <button 
                        onClick={() => setStep(1)} 
                        className="text-[#007AFF] font-bold text-[15px] hover:text-[#0062CC] transition-colors"
                      >
                        Back
                      </button>
                    </div>
                    
                    <div className="flex flex-col items-end gap-1">
                      {(() => {
                        const effectiveCount = createdDepartments.length + (deptName.trim() && createdDepartments.length < 5 ? 1 : 0);
                        const canContinue = true; // TEST OVERRIDE: Allow bypassing mandatory department rules
                        return (
                          <>
                            <button 
                              onClick={() => {
                                if (!canContinue) return;
                                if (deptName.trim() && createdDepartments.length < 5) {
                                  handleDeployDepartment();
                                }
                                handleNext();
                              }}
                              disabled={!canContinue}
                              className="px-[28px] py-[13.5px] bg-[#007AFF] hover:bg-[#0062CC] active:scale-[0.98] text-white font-bold rounded-[14px] shadow-[0_4px_12px_rgba(0,122,255,0.25)] transition-all text-[15px] disabled:opacity-50 disabled:cursor-not-allowed disabled:shadow-none"
                            >
                              Continue
                            </button>
                            {!canContinue && (
                              <span className="text-[11px] font-medium text-[#FF3B30]" style={{ color: '#FF3B30' }}>Add at least 1 department</span>
                            )}
                          </>
                        );
                      })()}
                    </div>
                  </div>
                </div>
              </div>
            </div>
          )}


          {/* Step 3: Onboard Employees */}
          {step === 3 && (
            <div className="w-full h-full flex flex-col bg-[#ffffff] animate-in fade-in duration-500">
              {/* Header */}
              <div className="w-full flex items-center justify-between px-10 py-8">
                <div className="flex items-center gap-4">
                  {/* Real Logo overlapping circles matching Phase 0 */}
                  <div style={{ position: 'relative', flexShrink: 0, marginTop: 4 }}>
                    <div style={{
                      width: 60, height: 60, borderRadius: '50%',
                      backgroundColor: getAvatarColor(companyName || 'Dort Asia').bg, overflow: 'hidden',
                      display: 'flex', alignItems: 'center', justifyItems: 'center', justifyContent: 'center', border: '1px solid rgba(0,0,0,0.05)'
                    }}>
                      {(croppedPreviewUrl || profilePic) ? (
                        <img src={croppedPreviewUrl ?? profilePic!} style={{ width: '100%', height: '100%', objectFit: 'cover' }} alt="Company" />
                      ) : (
                        <span className="avatar-initials" style={{ fontSize: 20, fontWeight: 800, color: getAvatarColor(companyName || 'Dort Asia').color }}>
                          {getInitials(companyName || 'DA')}
                        </span>
                      )}
                    </div>
                    <div style={{
                      position: 'absolute', bottom: -2, right: -2,
                      width: 26, height: 26, borderRadius: '50%',
                      backgroundColor: '#ffffff', display: 'flex',
                      alignItems: 'center', justifyContent: 'center', padding: 2,
                    }}>
                      <div style={{
                        width: '100%', height: '100%', borderRadius: '50%',
                        backgroundColor: getAvatarColor(`${adminFirstName} ${adminLastName}`.trim() || companyName || 'Dort Asia').bg, overflow: 'hidden',
                        display: 'flex', alignItems: 'center', justifyContent: 'center',
                      }}>
                        {(adminCroppedUrl || adminPic) ? (
                          <img src={adminCroppedUrl ?? adminPic!} style={{ width: '100%', height: '100%', objectFit: 'cover' }} alt="User" />
                        ) : (
                          <span className="avatar-initials" style={{ fontSize: 9, fontWeight: 800, color: getAvatarColor(`${adminFirstName} ${adminLastName}`.trim() || companyName || 'Dort Asia').color }}>
                            {adminFirstName
                              ? (adminFirstName[0] + (adminLastName?.[0] || '')).toUpperCase()
                              : getInitials(companyName || 'DA')}
                          </span>
                        )}
                      </div>
                    </div>
                  </div>
                  <div className="flex flex-col gap-0.5">
                    <span className="onboarding-dark-gray-text text-[11px] font-medium leading-none">Company</span>
                    <span className="text-[#000000] text-[16px] font-bold leading-none">{companyName || 'Dort Asia'}</span>
                  </div>
                </div>
                
                <div className="flex items-center gap-2 text-[14px]">
                  <span className="text-[#8E8E93] font-medium">Phase : </span>
                  <span className="text-[#007AFF] font-bold">3 out of 5</span>
                </div>
              </div>

              {/* Main Dual-Column Content */}
              <div className="flex w-full flex-1 border-t border-[#E5E5EA] overflow-hidden">
                {/* Left Side: Deployed Employees List */}
                <div className="w-[50%] flex flex-col px-[55px] py-[35px] border-r border-[#E5E5EA] h-full overflow-hidden relative">
                  <div className="mb-6 shrink-0">
                    <h3 className="text-[17px] font-bold !text-[#1C1C1E] flex items-center gap-2">
                    Deployed Employees <span className="text-[11px] font-bold text-[#FF9500] uppercase tracking-wider bg-[#FF9500]/10 px-2 py-[2px] rounded-md">(Max 15 While Setup)</span>
                  </h3>
                  <p className="text-[13px] font-medium !text-[#8E8E93] mt-1.5">
                    The Deployed Employees are partially onboarded, update them after Setup
                  </p>
                </div>
                <div className="flex-1 overflow-y-auto page-scrollbar pr-3 pb-20 space-y-3">
                  {deployedEmployees.length === 0 ? (
                    <div className="h-full flex flex-col items-center justify-center text-center px-4 mt-8 opacity-60">
                      <div className="w-16 h-16 rounded-full bg-[#F2F2F7] flex items-center justify-center mb-4">
                        <span className="text-2xl">??</span>
                      </div>
                      <p className="font-semibold text-[#8E8E93] text-[14px]">No employees added yet</p>
                      <p className="text-[12px] text-[#A1A1A6] mt-1 max-w-[200px]">Add your team members using the form to deploy them.</p>
                    </div>
                  ) : (
                    deployedEmployees.map(emp => (
                      <div key={emp.id} className="w-full bg-[#f8f9fa] rounded-[14px] p-3.5 px-4 flex items-center justify-between shadow-sm animate-in fade-in slide-in-from-left-2 duration-300 group hover:shadow-md transition-all border border-transparent hover:border-[#E5E5EA]">
                        <div className="flex items-center gap-4 flex-[2]">
                          <div className="h-10 w-10 shrink-0 rounded-full flex items-center justify-center shadow-sm" style={{ backgroundColor: getAvatarColor(emp.firstName).bg }}>
                            <span className="text-white font-bold text-[14px]">
                              {emp.firstName.charAt(0)}{emp.lastName.charAt(0)}
                            </span>
                          </div>
                          <div className="flex flex-col min-w-0">
                            <span className="text-[14px] font-bold !text-[#1C1C1E] truncate">{emp.firstName} {emp.lastName}</span>
                            <span className="text-[11px] font-medium !text-[#8E8E93] truncate">{emp.email}</span>
                          </div>
                        </div>

                        <div className="flex flex-col flex-1 pl-2 border-l border-[#E5E5EA]">
                          <span className="text-[13px] font-bold !text-[#1C1C1E] truncate">{emp.department}</span>
                          <span className="text-[11px] font-medium !text-[#8E8E93] truncate">{emp.designation}</span>
                        </div>

                        <div className="flex flex-col items-center px-4 min-w-[60px]">
                          <span className="text-[13px] font-semibold !text-[#1C1C1E]">{emp.gender}</span>
                        </div>
                        
                        <div className="flex flex-col items-end min-w-[80px]">
                          <span className="text-[13px] font-bold !text-[#1C1C1E] truncate">{emp.role}</span>
                        </div>

                        <button onClick={() => handleRemoveDeployedEmployee(emp.id)} className="ml-3 h-8 w-8 !bg-[#FFE5E5] hover:!bg-[#FFD1D1] rounded-full flex items-center justify-center transition-colors shrink-0">
                          <Trash2 className="h-4 w-4 !text-[#FF3B30]" />
                        </button>
                      </div>
                    ))
                  )}
                </div>
              </div>

              {/* Right Side: Form */}
              <div className="w-[50%] flex flex-col px-[55px] py-[35px] relative h-full overflow-hidden">
                <div className="flex-1 overflow-y-auto no-scrollbar pb-[100px] flex flex-col items-center">
                <div className="text-center mb-10 w-full">
                  <h2 className="text-[28px] font-extrabold !text-[#1C1C1E] mb-2 tracking-tight">Onboard Employees</h2>
                  <p className="text-[14px] font-medium !text-[#8E8E93]">Deploy Employees to their respective Department</p>
                </div>

                <div className="w-full max-w-[540px] mx-auto space-y-5">
                  <div className="flex gap-4">
                    <div className="relative flex-1 group">
                      <input type="text" value={empFirstName} onChange={(e) => setEmpFirstName(e.target.value.replace(/[^a-zA-Z\s]/g, ''))} placeholder="First Name" autoComplete="off" className="w-full h-[52px] px-5 bg-white border border-[#E5E5EA] rounded-[16px] text-[15px] font-semibold !text-[#1C1C1E] placeholder-transparent focus:outline-none focus:border-[#007AFF] focus:ring-4 focus:ring-[#007AFF]/10 transition-all peer" />
                      <label className="absolute left-[18px] -top-2.5 bg-white px-1.5 text-[11px] font-bold uppercase tracking-wider text-[#8E8E93] peer-focus:text-[#007AFF] transition-colors">Employee Name</label>
                      {!empFirstName && <span className="absolute left-5 top-1/2 -translate-y-1/2 text-[15px] font-medium text-[#C7C7CC] pointer-events-none peer-focus:opacity-0 transition-opacity">First Name</span>}
                    </div>
                    <div className="relative flex-1 group">
                      <input type="text" value={empLastName} onChange={(e) => setEmpLastName(e.target.value.replace(/[^a-zA-Z\s]/g, ''))} placeholder="Second Name" autoComplete="off" className="w-full h-[52px] px-5 bg-white border border-[#E5E5EA] rounded-[16px] text-[15px] font-semibold !text-[#1C1C1E] placeholder-transparent focus:outline-none focus:border-[#007AFF] focus:ring-4 focus:ring-[#007AFF]/10 transition-all peer" />
                      <label className="absolute left-[18px] -top-2.5 bg-white px-1.5 text-[11px] font-bold uppercase tracking-wider text-[#8E8E93] peer-focus:text-[#007AFF] transition-colors">Employee Name</label>
                      {!empLastName && <span className="absolute left-5 top-1/2 -translate-y-1/2 text-[15px] font-medium text-[#C7C7CC] pointer-events-none peer-focus:opacity-0 transition-opacity">Second Name</span>}
                    </div>
                  </div>

                  <div className="relative w-full group">
                    <input type="email" value={empEmail} onChange={(e) => setEmpEmail(e.target.value.replace(/[^a-zA-Z0-9@.]/g, '').trim())} placeholder="Enter Email" autoComplete="off" className="w-full h-[52px] px-5 bg-white border border-[#E5E5EA] rounded-[16px] text-[15px] font-semibold !text-[#1C1C1E] placeholder-transparent focus:outline-none focus:border-[#007AFF] focus:ring-4 focus:ring-[#007AFF]/10 transition-all peer" />
                    <label className="absolute left-[18px] -top-2.5 bg-white px-1.5 text-[11px] font-bold uppercase tracking-wider text-[#8E8E93] peer-focus:text-[#007AFF] transition-colors">Email Address</label>
                    {!empEmail && <span className="absolute left-5 top-1/2 -translate-y-1/2 text-[15px] font-medium text-[#C7C7CC] pointer-events-none peer-focus:opacity-0 transition-opacity">Enter Email</span>}
                  </div>

                  <div className="flex gap-4">
                    <div className="relative flex-1 group" style={{ zIndex: 102 }}>
                      <label className="absolute left-[18px] -top-2.5 bg-white px-1.5 text-[11px] font-bold uppercase tracking-wider text-[#8E8E93] z-10 transition-colors">Gender</label>
                      <CustomSelect value={empGender} onChange={setEmpGender} options={["Male", "Female", "Other"]} placeholder="Choose Gender" />
                    </div>
                    <div className="relative flex-1 group" style={{ zIndex: 102 }}>
                      <label className="absolute left-[18px] -top-2.5 bg-white px-1.5 text-[11px] font-bold uppercase tracking-wider text-[#8E8E93] z-10 transition-colors">Department</label>
                      <CustomSelect value={empDept} onChange={(val) => { setEmpDept(val); setEmpDesig(""); }} options={createdDepartments.map(d => d.name)} placeholder="Choose Dept" />
                    </div>
                  </div>

                  <div className="flex gap-4">
                    <div className="relative flex-1 group" style={{ zIndex: 101 }}>
                      <label className="absolute left-[18px] -top-2.5 bg-white px-1.5 text-[11px] font-bold uppercase tracking-wider text-[#8E8E93] z-10 transition-colors">Designation</label>
                      <CustomSelect value={empDesig} onChange={setEmpDesig} options={empDept ? (createdDepartments.find(d => d.name === empDept)?.designations || []) : []} placeholder="Choose Designation" />
                    </div>
                    <div className="relative flex-1 group" style={{ zIndex: 101 }}>
                      <label className="absolute left-[18px] -top-2.5 bg-white px-1.5 text-[11px] font-bold uppercase tracking-wider text-[#8E8E93] z-10 transition-colors">App Role</label>
                      <CustomSelect value={empRole} onChange={setEmpRole} options={["Admin", "Sub Admin", "Employee"]} placeholder="Choose Role" />
                    </div>
                  </div>

                  <div className="pt-2">
                    {errorLine && <p className="text-[#FF3B30] text-[13px] font-semibold text-center mb-4">{errorLine}</p>}
                  </div>
                </div>
              </div> {/* Close form scroll container */}

              {/* Absolute Bottom Navigation bar within the right pane */}
                <div className="absolute bottom-[35px] left-[55px] right-[55px] flex justify-between items-center bg-[#ffffff] pt-4 border-t-0">
                  <button onClick={() => { setStep(4); if (createdDepartments.length > 0 && !selectedOverviewDept) setSelectedOverviewDept(createdDepartments[0].name); }} className="text-[#007AFF] font-bold text-[16px] hover:text-[#0062CC] transition-colors leading-[48px]">
                    Next
                  </button>
                  <button onClick={handleAddEmployee} className="h-[48px] px-[32px] !bg-[#007AFF] hover:!bg-[#0062CC] active:scale-[0.98] text-white font-bold rounded-[14px] shadow-[0_4px_12px_rgba(0,122,255,0.25)] transition-all text-[15px]">
                    Add Employee
                  </button>
                </div>
              </div>
              </div>
            </div>
          )}
